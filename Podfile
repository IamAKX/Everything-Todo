# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'Everything Todo' do
  use_frameworks!

supports_swift_versions '< 5.0' # configure this for your project

pod 'Disk', '~> 0.6.3'
end
